# Diseño del refactor

Completá las tres secciones. Este archivo es parte de la entrega.

## 1. Responsabilidades

> _Listá las responsabilidades que identificaste en el código de partida y dónde vive
> cada una después del refactor. Una línea por responsabilidad. Reemplazá este texto._

## 2. Diagrama

> _Una caja por responsabilidad, una flecha por cada dato que pasa de una a otra.
> Puede ser texto (como el ejemplo de abajo) o una imagen. Reemplazá este texto._

```text
  [archivo] -> [parseo] -> [normalizacion] -> [estadisticas] -> [consola]
                                    |                \
                                    v                 -------> [reporte.txt]
                               [alarmas]
```

## 3. Hallazgos

> _Cosas que el programa hace de forma discutible y que **no** arreglaste porque
> hubieran cambiado la salida. Esta sección se evalúa. Reemplazá este texto._
